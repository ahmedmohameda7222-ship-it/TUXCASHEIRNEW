name: Task 5.1 Architecture RED

on:
  push:
    branches:
      - feat/ops-whatsapp-task5-1-red

jobs:
  red:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v7
        with:
          node-version: 24
      - name: Architecture RED
        run: node scripts/test-whatsapp-package-layering.mjs
