name: POS Refinement TDD

on:
  push:
    branches:
      - 'ui/tux-pos-refinement'

permissions:
  contents: read

concurrency:
  group: pos-refinement-tdd-${{ github.ref }}
  cancel-in-progress: true

jobs:
  tdd:
    name: fast-tdd
    runs-on: ubuntu-latest
    timeout-minutes: 12
    env:
      ELECTRON_SKIP_BINARY_DOWNLOAD: '1'
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v7
        with:
          node-version: '24'
          cache: npm
      - name: Install locked dependencies
        run: npm ci
      - name: Show worker preference retry formatter diffs
        shell: bash
        run: |
          npx prettier apps/operations/src/app/browserRemote.ts > /tmp/browserRemote.ts.formatted
          diff -u apps/operations/src/app/browserRemote.ts /tmp/browserRemote.ts.formatted || true
          npx prettier packages/application/src/workerUiPreferences.test.ts > /tmp/workerUiPreferences.test.ts.formatted
          diff -u packages/application/src/workerUiPreferences.test.ts /tmp/workerUiPreferences.test.ts.formatted || true
      - name: Format check
        run: npm run format:check
      - name: Lint
        run: npm run lint
      - name: Unit and integration tests
        run: npm run test
