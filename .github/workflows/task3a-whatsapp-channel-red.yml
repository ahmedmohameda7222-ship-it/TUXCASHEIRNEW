name: Task 3A WhatsApp Channel Migration RED

on:
  push:
    branches:
      - 'feat/ops-whatsapp-task3a-red'

permissions:
  contents: read

jobs:
  task3a-red:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v7
        with:
          node-version: '24'
      - name: WhatsApp channel migration RED
        run: node scripts/test-whatsapp-channel-migration.mjs
