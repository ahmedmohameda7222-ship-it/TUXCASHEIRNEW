name: Unified Menu Edit Patcher

on:
  push:
    branches:
      - ui/unified-menu-edit-mode
    paths:
      - .github/workflows/unified-menu-edit-patcher.yml

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
        with:
          ref: ui/unified-menu-edit-mode
          fetch-depth: 0
      - uses: actions/setup-node@v7
        with:
          node-version: '24'
          cache: npm
      - name: Install locked dependencies
        run: npm ci
      - name: Apply Task 6 production patch
        run: python .github/scripts/unified-menu-edit-patcher.py
      - name: Format Task 6 files
        run: |
          npx prettier --write apps/operations/src/app/OrdersWorkspace.tsx apps/operations/src/app/unifiedMenuEditMode.source.test.ts apps/operations/src/styles/final-pos-corrections.css
          git diff --check
      - name: Task 6 GREEN unit gate
        run: npm run test
      - name: Commit Task 6 production change
        run: |
          git config user.name "tux-implementation-bot"
          git config user.email "tux-implementation-bot@users.noreply.github.com"
          git add apps/operations/src/app/OrdersWorkspace.tsx apps/operations/src/app/unifiedMenuEditMode.source.test.ts apps/operations/src/styles/final-pos-corrections.css
          if ! git diff --cached --quiet; then
            git commit -m "feat: add keyboard reorder and edit jiggle"
            git push origin HEAD:ui/unified-menu-edit-mode
          fi
