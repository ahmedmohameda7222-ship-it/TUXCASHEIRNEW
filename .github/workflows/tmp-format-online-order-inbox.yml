name: Temporary shared online-order inbox formatter

on:
  push:
    branches:
      - work/menu-online-orders-integration
    paths:
      - .github/workflows/tmp-format-online-order-inbox.yml

jobs:
  format:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v7
        with:
          node-version: 24
          cache: npm
      - run: npm ci
        env:
          ELECTRON_SKIP_BINARY_DOWNLOAD: '1'
      - name: Print canonical formatter diff
        run: |
          npx prettier --write packages/application/src/onlineOrderInbox.ts
          git diff -- packages/application/src/onlineOrderInbox.ts
