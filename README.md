# TUX V2

Long-term TypeScript rebuild of TUX Operations. Product implementation is developed on `integration/tux-operations-v2`; `main` is reserved for approved release integration.

The legacy `ahmedmohameda7222-ship-it/Tuxcashier` repository is read-only reference only.
